fun add(a: i32[1], b: i32[1]): i32 {
    return a[0] + b[0];
}

fun Main(args: string[*]): i32 {
    foo: i32[1] = { 2 };
    bar: i32[1] = { 4 };

    assert(add(foo, bar) == 6, "add(foo, bar) == 6");
    return 0;
}